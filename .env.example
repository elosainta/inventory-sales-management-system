APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=isms
DB_USERNAME=app_user
DB_PASSWORD=changeme
DB_ROOT_PASSWORD=changeme_root
# Isolated sandbox DB for demo/training logins (see UseDemoDatabase middleware).
# Provision once as root: CREATE DATABASE + GRANT to the app user, then
# `php artisan demo:reset` to clone the live data in.
DB_DEMO_DATABASE=isms_demo

# Session/cache/queue must stay pinned to the live DB so that swapping a demo
# login onto the demo connection never sends its session or cache there.
SESSION_CONNECTION=mysql
DB_CACHE_CONNECTION=mysql
DB_CACHE_LOCK_CONNECTION=mysql
DB_QUEUE_CONNECTION=mysql

CLOUDFLARE_TUNNEL_TOKEN=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

SUPPORT_EMAIL=

# Invoice scan (BETA) - web upload -> Claude reads the photo -> Bukku bill.
# Both are secrets: set them in the server .env, never commit real values.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5
BUKKU_API_TOKEN=
BUKKU_BASE_URL=https://api.bukku.my
BUKKU_SUBDOMAIN=
BUKKU_DEFAULT_ACCOUNT_ID=33
